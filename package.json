{
  "name": "urent-workspace",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"bgBlue.bold,bgGreen.bold\" --names \" CLIENT \",\" SERVER \" \"npm run dev --prefix urent-client\" \"npm run dev --prefix urent-server\"",
    "dev:client": "npm run dev --prefix urent-client",
    "dev:server": "npm run dev --prefix urent-server",
    "build": "concurrently --prefix-colors \"bgBlue.bold,bgGreen.bold\" --names \" CLIENT \",\" SERVER \" \"npm run build --prefix urent-client\" \"npm run build --prefix urent-server\"",
    "check": "concurrently --prefix-colors \"bgBlue.bold,bgGreen.bold\" --names \" CLIENT \",\" SERVER \" \"npm run typecheck --prefix urent-client\" \"npm run typecheck --prefix urent-server\"",
    "env:vercel": "powershell -ExecutionPolicy Bypass -File ./scripts/sync-vercel-env.ps1"
  },
  "devDependencies": {
    "@types/swagger-ui-express": "^4.1.8",
    "concurrently": "^9.2.1",
    "tsx": "^4.22.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "firebase-admin": "^13.10.0",
    "swagger-ui-express": "^5.0.1"
  }
}
